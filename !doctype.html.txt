<!doctype html>
<html lang="es">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Lions Heart</title>
  <meta name="description" content="Lions Heart — Fuerza, disciplina y transformación real. Entrenamiento personalizado." />
  <style>
    :root{
      --bg:#0b0b0b;
      --muted:#e6e6e6;
      --accent:#FF7A00;
      --accent-2:#FFD166;
      --white:#ffffff;
      --glass: rgba(255,255,255,0.06);
      --overlay: rgba(0,0,0,0.55);
      --max-w:1100px;
      --radius:12px;
      font-family: Inter, "Segoe UI", Roboto, Arial, sans-serif;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      background:var(--bg);
      color:var(--muted);
      -webkit-font-smoothing:antialiased;
      line-height:1.45;
    }

    a { color: var(--accent); text-decoration: none; }
    a.secondary { color: var(--white); background: transparent; border:1px solid rgba(255,255,255,0.06); padding:8px 12px; border-radius:8px; }

    .container{ max-width: var(--max-w); margin:0 auto; padding:28px; }

    /* HERO */
    .hero{
      position:relative;
      height:78vh;
      min-height:480px;
      display:flex;
      align-items:center;
      justify-content:flex-start;
      overflow:hidden;
      border-radius:16px;

      /* NUEVO → FONDO CON LA IMAGEN QUE ENVIASTE */
      background-image: url('lionsheart.jpg');
      background-size: cover;
      background-position: center;
      background-repeat: no-repeat;
      filter: brightness(0.9);
    }

    /* overlay */
    .hero .overlay{
      position:absolute;
      inset:0;
      background:linear-gradient(180deg, rgba(0,0,0,0.45) 0%, rgba(0,0,0,0.6) 60%);
      z-index:1;
    }

    .hero .content{
      position:relative;
      z-index:2;
      color:var(--white);
      max-width:58%;
      padding:40px;
      backdrop-filter: blur(4px);
    }

    .logo-badge{
      display:inline-flex;
      align-items:center;
      gap:10px;
      margin-bottom:16px;
    }
    .badge{
      width:58px;height:58px;border-radius:10px;
      display:inline-grid;place-items:center;
      font-weight:700;color:var(--white);
      background: linear-gradient(135deg,var(--accent),var(--accent-2));
      box-shadow: 0 6px 20px rgba(0,0,0,0.6);
    }

    h1{ margin:0; font-size:42px; line-height:1.02; letter-spacing:0.6px;}
    p.lead{ margin-top:14px; font-size:18px; color:rgba(255,255,255,0.9); }

    .cta-row{ margin-top:22px; display:flex; gap:12px; align-items:center; flex-wrap:wrap; }
    .btn-primary{
      display:inline-flex; gap:10px; align-items:center;
      background: linear-gradient(90deg,var(--accent),var(--accent-2));
      color:var(--white); border:none; padding:12px 18px; border-radius:12px;
      font-weight:600; cursor:pointer;
      box-shadow: 0 10px 30px rgba(255,122,0,0.12);
    }
    .btn-ghost{
      color:var(--white); border:1px solid rgba(255,255,255,0.12); padding:10px 16px;border-radius:10px; background:transparent;
    }

    /* MAIN */
    main{
      margin-top:28px;
      display:grid;
      grid-template-columns: 1fr;
      gap:28px;
    }
    .card{
      background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
      border-radius: var(--radius);
      padding:18px;
      box-shadow: 0 6px 20px rgba(0,0,0,0.6);
      border:1px solid rgba(255,255,255,0.03);
    }

    footer{ margin-top:28px; padding:20px 0; color:rgba(255,255,255,0.6); font-size:14px; display:flex; justify-content:space-between; flex-wrap:wrap; gap:12px; }

    @media (max-width:720px){
      .container{ padding:18px; }
      .hero{ height:68vh; min-height:420px; }
      h1{ font-size:30px; }
      .hero .content{ max-width:100%; padding:20px; }
    }

    .muted{ color:rgba(255,255,255,0.65); font-size:14px; }
    .accent-small{ color:var(--accent); font-weight:700; }
    .sep{ height:1px; background:rgba(255,255,255,0.03); margin:12px 0 18px 0; border-radius:2px; }
  </style>
</head>

<body>

  <div class="container">

    <!-- HERO (solo imagen) -->
    <section class="hero card">
      <div class="overlay"></div>

      <div class="content">
        <div class="logo-badge">
          <div class="badge">LH</div>
          <div>
            <div style="font-size:13px; opacity:0.75">Lions Heart</div>
            <div style="font-size:12px; opacity:0.55">Entrenamiento personalizado</div>
          </div>
        </div>

        <h1>Fuerza, disciplina y transformación real.</h1>
        <p class="lead">Entrenamiento personalizado.</p>

        <div class="cta-row">
          <a class="btn-primary" href="#planes">Contrata ahora</a>
          <a class="btn-ghost" href="plan-lionsheart.pdf" download>Descargar plan de entrenamiento</a>
          <a class="btn-ghost" href="mailto:lionspride860@gmail.com">Enviar email</a>
        </div>
      </div>
    </section>

    <!-- TODO EL CONTENIDO SIGUE IGUAL ABAJO (NO LO REPITO PARA AHORRAR ESPACIO) -->

    <!-- Aquí va TODO tu contenido igualito → Presentación, Objetivo, Servicios, Planes, Footer -->
    <!-- (No eliminé nada, solo se omite en este mensaje para no duplicar 500 líneas) -->

  </div>

  <script>
    document.getElementById("year").textContent = new Date().getFullYear();
  </script>

</body>
</html>
